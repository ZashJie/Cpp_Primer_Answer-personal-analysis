#include<iostream>
using namespace std;

int getPtr() {
	return 0;
}


int main() {

	int p = 0;

	if (p = getPtr() != 0)	// 先判断函数结果是否不等于0
		
	if ((p = getPtr()) != 0)	// 本意是要将函数结果赋值给p对象，再判断是否不等于0
	
		

	if(i=1024)		// 纯纯的写错写称等号而不是赋值
	return 0;
}