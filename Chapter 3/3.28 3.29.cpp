#include <iostream>
#include <vector>
using namespace std;

string sa[10];
int ia[10];

int main() {

	
	string sa2[10];
	int ia2[10];

	// 标准答案： 数组的元素会被默认初始化。sa 的元素值全部为空字符串，ia 的元素值全部为0。
	// sa2 的元素值全部为空字符串，ia2 的元素值全部未定义。


	// 3.29 相比于vector来说，数组有哪些缺点
	/*
	* 数组的大小确定不变，不能随意向数组中添加元素，所以如果遇到
	* 不清楚元素的确切个数的情况，请使用 vector
	* 
	* 标准答案：
	* 数组的大小是确定的。
	  不能随意增加元素
      不允许拷贝和赋值
	

	*/ 

	return 0;
}